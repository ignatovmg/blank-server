
def validate(user_dir, user_fixed_dir):
    """
    Fixes data in user_dir and puts it into user_fixed_dir
    :return: List of errors strings. Empty list of no errors.
    :rtype: list(str)
    """

    return [] #['Fake Error']
